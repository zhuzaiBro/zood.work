version: '3.8'

services:
  blog-fe:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "13000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
    networks:
      - blog-network

networks:
  blog-network:
    driver: bridge

